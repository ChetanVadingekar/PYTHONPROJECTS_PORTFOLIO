reference test case

def test_format_project_details_with_valid_rec_year_month_date():
    """

    Test case for when 'rec_year_month_date' is a valid date string and needs to be formatted.

    """
    from utility import format_project_details
    projects = [{
        "project_name": "Test Project",
        "country_code": "JP",
        "generation_name": "1.0",
        "model_name": "ADAS",
        "drive_session_table_name": "test-drive-session-table"
    }]

    with patch("opensearch_utility.DriveSessionUtility.get_rec_year_month_date") as mock_get_rec_date:
        mock_get_rec_date.return_value = "20240814"
        result = format_project_details(projects)
        rec_year_month_date = result[0]['projects'][0]['rec_year_month_date']
        expected_date = "2024-08-14"
        assert rec_year_month_date == expected_date
        assert result[0]['projects'][0]['project_name'] == "Test Project"
        assert result[0]['projects'][0]['version'] == "Gen1.0 ADAS"

so usually we write test case like this.
