log_content = "\n".join(
                f"{datetime.fromtimestamp(event['timestamp']/1000).isoformat()}:"
                f"{event['message']}"
                for event in logs
            )

we are doing static code analysis and i am getting one error as the second line is exceeding to 79 
how to resolve it ?
