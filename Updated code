            if rec_year_month_date != "Unavailable":
                rec_year_month_date = datetime.strptime(
                    rec_year_month_date, "%Y%m%d"
                ).strftime("%Y-%m-%d")

we are handling the query response like this

now if the query would return text "hstek sjwlk" how we can handle here
